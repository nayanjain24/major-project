# dataset/

This folder stores all sign-language data used by the project.

Suggested layout:
- `raw/`: original captured videos/images
- `processed/`: cleaned landmarks/features ready for model input
- `labels/`: label files or metadata (CSV/JSON)

Keep large datasets out of git unless intentionally versioned.
